server:
  port: 8001
spring:
  application:
    name: provider-ticket
eureka:
  instance:
    prefer-ip-address: true     # 根据服务的ip地址注册服务
  client:
    service-url:
      {defaultZone: http://localhost:8761/eureka/}
